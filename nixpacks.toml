providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "corepack enable",
  "corepack prepare yarn@4.9.4 --activate",
  "yarn install --immutable"
]

[phases.build]
cmds = ["yarn build"]

[start]
cmd = "yarn start"
